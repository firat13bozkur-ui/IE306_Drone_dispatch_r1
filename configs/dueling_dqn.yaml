env_id: DroneDispatch-v0
eval_config: configs/eval_standard.yaml

seed: 0
total_episodes: 500
max_steps_per_episode: 1000

gamma: 0.99
reward_scale: 0.1
learning_rate: 0.0003
batch_size: 64
replay_size: 50000
min_replay_size: 1000

epsilon_start: 1.0
epsilon_end: 0.05
epsilon_decay_steps: 15000

target_update_freq: 1000
train_freq: 4

hidden_dim: 256

prefer_assignment: true
charge_threshold: 0.0

save_path: weights/dueling_dqn_seed0.pt
log_path: logs/dueling_dqn_seed0.csv
eval_output_path: logs/dueling_dqn_eval_seed0_1_2.json